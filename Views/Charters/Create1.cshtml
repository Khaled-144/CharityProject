@model CharityProject.Models.charter

<!DOCTYPE html>
<html lang="ar">
<head>
    <meta charset="UTF-8" dir="rtl">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>عهدة استلام</title>

    @*     <script>
    function toggleElements() {
    const choice = document.getElementById('choice').value;
    const general = document.getElementById('h');
    const custom = document.getElementById('m');

    if (choice === 'عام') {
    general.style.display = 'block';
    custom.style.display = 'none';
    } else if (choice === 'مخصص') {
    general.style.display = 'block';
    custom.style.display = 'block';
    } else {
    general.style.display = 'none';
    custom.style.display = 'none';
    }
    }



    </script> *@


    @*     <style>
    .general, .custom {
    display: none;
    }

    </style> *@





</head>




<body>




@*     Old but Gold *@     



 
    <div class="container mx-auto px-6 py-8">
        <div class="flex flex-col bg-white shadow-md rounded-lg overflow-hidden md:flex-row">
            <div class="w-full md:w-1/4 px-5 py-8">

                <form asp-action="Create1" method="post" calss="flex items-center justify-center form-group ">


                    <h2 class="text-2xl flex items-center justify-center font-bold text-gray-800 mb-4">نموذج عهده</h2>

                    <hr class="text-1xl" />


                    <div class=" flex  items-center   mt-3 mb-4">

                        <label asp-for="charter_info" for="charter_info" class="font-bold pl-3 text-sm font-medium text-gray-700 " required>معلومات العهدة  </label>
                        <input asp-for="charter_info" type="text" class=" pl-3  w-64 rounded-md border border-gray-300 focus:outline-none focus:ring-1 focus:ring-blue-500" required>

                        <label asp-for="serial_number" for="serial_number" class="pr-5  pl-3    font-bold font-medium text-gray-700">الرقم التسلسلي</label>
                        <input asp-for="serial_number" type="text" id="serialNumber" class=" pl-3 w-64 rounded-md border border-gray-300 focus:outline-none focus:ring-1 focus:ring-blue-500">

                    </div>


                    <div class="mb-2  flex  items-center ">
                        <label asp-for="creation_date" for="receivedDate" class=" pl-3 font-bold text-sm font-medium text-gray-700" required>تاريخ الانشاء</label>
                        <input asp-for="creation_date" type="date" id="myDateField" class=" pl-3 w-64  rounded-md border border-gray-300 focus:outline-none focus:ring-1 focus:ring-blue-500">




                        <label asp-for="from_departement_name" for="section" class=" pl-3 pr-20 font-bold text-sm font-medium text-gray-700">القسم</label>
                        <input asp-for="from_departement_name" type="text" id="section" class="pl-3 pr-20 w-64 rounded-md border border-gray-300 focus:outline-none focus:ring-1 focus:ring-blue-500">
                    </div>






                    <div class="mb-3 mt-4   flex  items-center ">
                        <label asp-for="status" for="deliveredStatus" class="  pl-3 font-bold text-sm font-medium text-gray-700" required>حالة التسليم</label>
                        <input asp-for="status" type="text" id="deliveredStatus" class=" pl-3 w-64  rounded-md border border-gray-300 focus:outline-none focus:ring-1 focus:ring-blue-500" placeholder="تم التسليم">




                        <label asp-for="notes" for="remarks" class="pl-3 pr-20 font-bold text-sm font-medium text-gray-700">ملاحظات</label>
                        <input asp-for="notes" type="text" id="remarks" class="pl-3 pr-20 w-64  rounded-md border border-gray-300 focus:outline-none focus:ring-1 focus:ring-blue-500" />
                    </div>




@* 
                   <div class="mt-3 flex px-5   form-group">


                        <div id="h" class="general px-5 flex">
                            <label asp-for="to_departement_name" for="department" class="  font-bold font-medium text-gray-700">القسم</label>
                            <select asp-for="to_departement_name" id="department" name="department" class="font-bold border w-44 rounded-md focus:ring-blue-500 focus:border-blue-500" asp-items="@(new SelectList(ViewData["Departments"] as IEnumerable<dynamic>, "departement_id", "departement_name"))">
                    <option value="">-- أختر القسم  --</option>>
                                
                            </select>
                        </div>

                        <div id="m" class="custom px-5 flex>
                            <label  for="to_emp_id"    class="mr-4 px-4  font-bold  font-medium text-gray-700">الموظف</label>
                    <select asp-for="to_emp_id" id="to_emp_id" name="to_emp_id" class="font-bold   border w-24 rounded-md focus:ring-blue-500 focus:border-blue-500"    asp-items="@(new SelectList(ViewData["Employees"] as IEnumerable<dynamic>, "employee_id", "name"))">
                    <option value="">-- أختر الموظف --</option>>
                             
                            </select>
                        </div>
                    </div> *@     

                    <div id="h" class="general px-5 flex">
                        <label asp-for="to_departement_name" class="font-bold font-medium text-gray-700">القسم</label>
                        <select asp-for="to_departement_name"
                                asp-items="@(ViewData["Departments"] as IEnumerable<SelectListItem>)"
                                class="font-bold border w-44 rounded-md focus:ring-blue-500 focus:border-blue-500">
                            <option value="">-- أختر القسم --</option>
                        </select>
                    </div>

                    <div class="form-group">
                        <label asp-for="to_emp_id" class="control-label">الموظف</label>
                        <select asp-for="to_emp_id"
                                asp-items="@(ViewData["Employees"] as IEnumerable<SelectListItem>)"
                                class="form-control">
                            <option value="">-- اختر الموظف --</option>
                        </select>
                    </div>
 

                 

                  @*   <div class="form-group">
                        <label asp-for="from_departement_name" class="control-label"></label>
                        <select asp-for="from_departement_name" class="form-control"
                                asp-items="@(new SelectList(ViewData["Departments"] as IEnumerable<dynamic>, "departement_id", "departement_name"))">
                            <option value="">-- Select Department --</option>
                        </select>
                    </div>

                    <div class="form-group">
                        <label asp-for="to_emp_id" class="control-label"></label>
                        <select asp-for="to_emp_id" class="form-control"
                                asp-items="@(new SelectList(ViewData["Employees"] as IEnumerable<dynamic>, "employee_id", "name"))">
                            <option value="">-- Select Employee --</option>
                        </select>
                    </div>
 *@

                    <div class="flex justify-center form-group  mt-4">
                        <button type="submit"     class="bg-gradient-to-r from-green-400 via-green-500 to-green-600 hover:bg-gradient-to-br focus:ring-4 focus:outline-none focus:ring-green-300 dark:focus:ring-green-800 font-medium rounded-lg text-sm px-5 py-2.5 text-center me-2 mb-2 text-white font-bold py-2 px-4 transition duration-300 ease-in-out">إرسال</button>
                    </div>

                </form>


            </div>
        </div>
    </div>     


@*      newwwwwwwwwwwwwwwwwwwwwwwwwwwwww  ////////// *@






</body>





</html>
@* 
<script>
    // Get today's date
    let today = new Date();

    // Format the date to YYYY-MM-DD (required by input type="date")
    let dd = String(today.getDate()).padStart(2, '0');
    let mm = String(today.getMonth() + 1).padStart(2, '0'); // January is 0!
    let yyyy = today.getFullYear();

    let formattedDate = yyyy + '-' + mm + '-' + dd;

    // Set the value of the date input field
    document.getElementById('myDateField').value = formattedDate;
</script> *@     



<script>
    document.addEventListener('DOMContentLoaded', function () {
        let today = new Date();
        let dd = String(today.getDate()).padStart(2, '0');
        let mm = String(today.getMonth() + 1).padStart(2, '0'); // January is 0!
        let yyyy = today.getFullYear();
        let formattedDate = yyyy + '-' + mm + '-' + dd;
        document.getElementById('myDateField').value = formattedDate;
    });
</script>